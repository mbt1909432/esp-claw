# Nova ESP32 Agent 改造规划

> 实施状态（2026-09-21）：第一阶段代码已完成，前端类型检查与生产构建通过；待 ESP-IDF 环境中的固件编译和真机烧录验证。

## 0. 当前进度

- [x] 定义 Nova / Elon Xu 身份边界和四种人格。
- [x] 增加 NVS 配置、HTTP API、长度检查与旧设备默认值。
- [x] 动态组合固定规则、身份信息和人格提示词。
- [x] 增加中英文 Agent 设置页、导航、脏状态、保存和重启提示。
- [x] 替换网页标题、设备日志、默认 AP 前缀与 mDNS 名称。
- [x] 制作图标母版、128×128 Web 资源和 64×64 favicon。
- [x] 完成桌面与移动端页面预览。
- [ ] 在 ESP-IDF 环境编译固件。
- [ ] 烧录设备并验证 NVS 迁移、重启生效和 Agent 自我介绍。

## 1. 身份边界

- 产品名称：**Nova ESP32 Agent**
- Agent 名称：**Nova**
- 主人姓名：**Elon Xu**
- 默认称呼：**Elon**
- Nova 是运行在 ESP32 上的 Agent，不把自己说成 Elon Xu，也不借用 Elon Musk 的人物形象。
- 项目继续保留原 ESP-Claw 的作者信息、Apache-2.0 许可证和上游来源说明。

## 2. 改造目标

1. 用户打开配置页时，看到统一的 Nova 品牌、图标和设备名称。
2. 用户可以设置 Agent 名称、主人姓名、主人称呼和初始人格。
3. 系统提示词由“不可覆盖的工具与安全规则 + 身份信息 + 人格 + 运行角色”组合生成。
4. 自定义人格只能补充语气、解释方式和工作偏好，不能替换工具规则、安全约束或文件处理规则。
5. 旧设备升级后自动获得默认值，不要求清空 NVS，也不破坏已有网络、模型和 IM 配置。

## 3. 第一阶段范围

### 3.1 配置字段

| 字段 | 默认值 | 用途 |
| --- | --- | --- |
| `agent_display_name` | `Nova` | Agent 对外自称 |
| `agent_owner_name` | `Elon Xu` | 主人完整姓名 |
| `agent_owner_address` | `Elon` | 对话中称呼主人 |
| `agent_persona_id` | `hardware_mentor` | 预设人格 ID |
| `agent_custom_prompt` | 空 | 自定义人格补充，限制长度 |

自定义人格单独使用约 1-2 KB 的容量上限，不扩大全部通用配置字符串，以免无谓增加 ESP32 内存占用。

### 3.2 人格预设

- `hardware_mentor`：耐心的硬件导师。先解释电流、引脚和接线关系，再给操作步骤；遇到不确定的板卡型号先确认，不冒险给出可能烧坏器件的接法。
- `concise_engineer`：直接的调试工程师。优先给结论、检查顺序、预期现象和最小修复方案。
- `friendly_assistant`：友好的通用助手。语气自然，必要时补充背景，但不堆砌术语。
- `custom`：在不可覆盖规则之后追加用户自定义说明。

### 3.3 提示词组合

运行时按以下顺序生成 system prompt：

1. 不可覆盖的工具、权限、安全和文件处理规则；
2. Nova 的身份、主人身份和称呼；
3. 选中的人格预设或自定义人格；
4. 记忆、技能和长期上下文规则；
5. Root Agent 或 Subagent 的角色规则。

人格修改第一版采用“保存后重启生效”。当前核心的配置热更新不会替换正在运行的 system prompt；贸然热改容易造成旧指针仍在使用。后续若需要无重启切换，应在核心层增加带锁的原子 prompt 更新接口。

### 3.4 网页配置页

在“系统设置”中增加“Agent 设置”页面，包含：

- Agent 名称输入框；
- 主人姓名输入框；
- 主人称呼输入框；
- 人格预设选择；
- 仅在“自定义”模式显示的多行文本框和字数提示；
- 保存、放弃修改和重启生效提示。

页面沿用现有 SolidJS 组件、表单、脏状态管理和中英文翻译，不另建一套视觉系统。

### 3.5 品牌与设备名称

首批替换用户可见内容：

- 浏览器标题、侧栏标题、状态栏标识；
- 默认 AP 名称：`nova-agent-<MAC>`；
- mDNS 实例名：`Nova ESP32 Agent`，主机名优先使用 `nova-agent.local`；
- 启动日志中的产品名称；
- Web 聊天页中对 Agent 的称呼；
- favicon 和网页头部图标。

以下内容保持不变：内部 `claw_*` API、组件目录、NVS 命名空间、能力 ID、已有存储路径和许可证头。这些是兼容接口，不属于对外品牌。

## 4. 图标方向

- 透明背景的方形图标，母版 1024×1024；
- 主体是紧凑的发光 **N 形芯片 / Agent 核心**；
- 青色与白色为主，少量黄色信号点缀；
- 不使用人物、龙虾、Elon Musk 形象或图标内文字；
- 轮廓在 32×32 仍能辨认；
- 从母版导出 Web PNG、favicon 和设备端兼容版本。

## 5. 实施顺序

1. 增加配置结构、默认值、NVS 读写、校验和 HTTP JSON 字段。
2. 在 `app_claw` 中组合动态 system prompt，并把配置映射到运行时。
3. 新增 Agent 设置页，接入导航、脏状态和中英文文案。
4. 生成并接入 Nova 图标，替换网页可见品牌和默认设备名。
5. 执行前端类型检查与构建，检查压缩后的内嵌页面资源。
6. 执行 C 代码静态检查、`git diff --check` 和配置迁移检查。
7. 在具备 ESP-IDF 的环境中完成固件编译、烧录、重启生效和真机回归。

## 6. 验收清单

- 新设备首次启动显示 Nova 默认身份。
- 已配置设备升级后保留原模型、网络和 IM 设置，并补齐 Nova 默认字段。
- 四种人格均能保存；自定义人格超长时给出明确错误。
- 保存人格后页面明确提示重启生效，重启后 system prompt 使用新值。
- Agent 不把主人 Elon Xu 当成自己。
- 用户自定义文本不能覆盖工具、安全和权限规则。
- 中英文界面无残留的主要 ESP-Claw 品牌文字。
- 图标在桌面、手机和 32×32 favicon 尺寸均清晰。
- `pnpm typecheck`、`pnpm build`、`git diff --check` 通过。
- 固件构建若因本机缺少 ESP-IDF 未执行，交付时明确标注。

## 7. 后续阶段

- 增加安全的 system prompt 热更新，取消重启要求。
- 支持人格配置导入、导出和恢复默认。
- 在设备信息页展示板卡型号、Flash、PSRAM、固件版本和当前人格。
- 为不同硬件教学场景增加可安装的人格包，而不是继续扩大基础固件提示词。
