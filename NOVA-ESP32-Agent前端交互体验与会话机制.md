# Nova ESP32 Agent 前端交互体验与会话机制

> 记录日期：2026-09-21
> 状态：体验分析与后续改造建议，尚未实施本文件中的前端改造。

## 1. 这次分析的结论

当前前端的主要问题不是功能完全缺失，而是“系统状态、下一步动作和失败后的处理方式”没有始终讲清楚。用户需要自己猜：设备是否在线、模型是否配置好、图片是否真的被读取、文件是否会一直占用 ESP32 存储，以及聊天刷新后上下文是否还在。

后续改造应围绕一句话展开：

> 用户每做一步，都能马上知道“发生了什么、还缺什么、下一步点哪里”。

## 2. 全局前端体验建议

### 2.1 增加统一的设备状态条

建议在页面顶部持续显示：

- Nova ESP32 Agent 是否在线；
- Wi-Fi 是否连接、当前 IP；
- 当前模型名称；
- 是否支持视觉输入；
- Agent 是否已启动并绑定本地 IM；
- 存储剩余空间和临时文件状态。

“在线”不应只代表浏览器 WebSocket 已连接，而应拆开显示“设备在线、聊天通道已连接、Agent 已绑定、模型已配置、视觉能力可用”等状态。

### 2.2 错误信息要带解决动作

不要只显示 `Failed to load config` 或 `Failed to scan nearby Wi-Fi`。应说明原因和下一步，例如：

```text
设备接口暂时无法访问。
请确认电脑和 ESP32 在同一个网络，或连接 ESP32 的配网热点。
[重新连接]
```

扫描失败、模型未配置、设备未绑定、文件过大和视觉模型不支持，应分别给出不同提示，不能都归为“发送失败”。

### 2.3 设置页的交互方向

Wi-Fi 设置可以保持现在“ESP32 扫描、点击热点回填 SSID”的方向，再补充：

- 信号图标和加密锁图标；
- “已选择”状态；
- 上次扫描时间；
- 扫描结果数量；
- 明确提示密码不会自动获取，仍需手动输入；
- “保存并测试连接”，让用户得到明确的连接结果。

底层的 `WPA2_PSK` 等枚举不应直接展示给普通用户，可翻译成“WPA2 加密、信号较强、信道 6”等信息。

## 3. 在线聊天页面的体验建议

当前 Web 在线聊天已经具备 WebSocket、自动重连、附件上传、Markdown 预览和失败重试等基础能力，但还需要把这些能力解释给用户。

### 3.1 空状态要告诉用户能做什么

没有消息时，除了“尚无消息”，建议提供可点击示例：

- 帮我解释这块 ESP32 开发板的引脚；
- 这张电路图里的电流怎么走？；
- 帮我检查这个 LED 接线；
- 读取我刚上传的 TXT / MD 文件。

### 3.2 附件必须可见、可管理

目前页面只显示“待发送附件数量”，用户看不到具体文件。建议改为附件预览区，显示：

- 图片缩略图或文档图标；
- 文件名和大小；
- 上传中、已准备、失败三种状态；
- 移除附件；
- 重新上传；
- 图片压缩进度。

选择图片或文档后的流程应让用户看见：

```text
选择文件 → 浏览器检查大小和类型 → 必要时压缩 → 上传到临时区 → 发送消息
```

### 3.3 明确“文件是否真的被模型理解”

消息旁边应区分这些情况：

- 图片已发送，当前模型支持视觉理解；
- 图片已上传，但当前模型不支持视觉输入；
- TXT / MD 已读取并作为本次消息上下文；
- 文件上传成功，但 Agent 读取失败；
- 文件太大，浏览器或设备已拒绝；
- 设备临时存储空间不足。

“文件已上传”不等于“模型已经读懂”，这两个状态需要在界面上明确区分。

### 3.4 大文件只做临时附件

为避免 16 MB Flash 被图片持续占满，建议以后采用：

1. 浏览器端先检查和压缩图片；
2. 只把本次消息需要的文件上传到 `/inbox/webim`；
3. 发送完成后由设备清理临时文件；
4. 页面明确提示“附件只作为本次消息的临时文件，不会长期保存到设备”。

这类临时文件清理应有成功、失败和待清理状态，不能让用户猜文件是否还在 Flash 中。

### 3.5 聊天记录和消息流

当前 Web 页面中的消息列表是内存状态，刷新页面后界面历史会消失；服务端没有 WebSocket 历史回放。建议：

- 默认把聊天记录保存在浏览器 IndexedDB，而不是 ESP32 Flash；
- 增加“清空当前对话”；
- 增加“导出为 Markdown”；
- 流式 assistant 消息按同一条消息合并到一个气泡中；
- 显示“Nova 正在思考 / 正在读取文件 / 正在调用工具”等过程状态。

### 3.6 输入框快捷键

普通用户更容易理解 `Enter` 发送、`Shift + Enter` 换行。若继续使用 `Ctrl + Enter`，必须在输入框附近长期显示快捷键提示。

## 4. 会话到底是什么

这里有四个容易混淆的“会话”：

| 名称 | 含义 | 断线或刷新后是否必然变化 |
| --- | --- | --- |
| WebSocket 连接 | 浏览器和 ESP32 之间的一条实时网络连接 | 会变化。重连会建立新的连接 |
| Web `chat_id` | 网页给当前聊天分配的 UUID | 正常重连、刷新不会变化，保存在浏览器 `localStorage` |
| Agent 会话 ID | LLM 上下文和历史记录使用的设备端会话 | 通常由“来源通道 + chat_id + 当前会话别名”决定 |
| 页面消息列表 | 浏览器当前内存里显示的气泡 | 刷新页面会丢失，目前没有服务端历史回放 |

因此，“每次重新连接都会新建一个 session”这个说法需要拆开看：

- **WebSocket 层面：是新的连接。**
- **聊天层面：通常不是新的聊天会话。** 只要浏览器仍保留原来的 `chat_id`，后端收到的仍是同一个 Web 聊天上下文。
- **界面显示层面：刷新后会像新聊天。** 因为当前消息列表只保存在内存中，即使设备端会话历史仍然存在，页面也不会自动把旧消息加载回来。

## 5. Web 在线聊天的实际会话规则

当前实现位于 `frontend_source/src/pages/WebImPage.tsx`：

- 使用 `localStorage` 键 `esp-claw-webim-chat-id` 保存 Web `chat_id`；
- 首次使用时生成一个 UUID；
- WebSocket 建立或重连时，都会发送同一个 `chat_id` 的 `hello`；
- HTTP 发送消息时也会提交同一个 `chat_id`；
- Web 通道在设备端固定标识为 `web`；
- Web 的后端聊天上下文由 `source_channel=web` 和该 `chat_id` 共同区分；
- WebSocket 断开、自动重连、关闭再打开页面，通常不会自动切换到新的聊天上下文。

以下情况会导致 Web 使用新的聊天上下文：

- 清除了浏览器站点数据或 `localStorage`；
- 换了浏览器、浏览器用户配置、无痕窗口或不同的站点来源；
- 手动实现“新建对话”并生成新的 `chat_id`；
- 设备端的会话存储被删除或重置。

多个同源浏览器标签页可能共用同一个 `chat_id`，因此会落到同一个后端聊天上下文；但每个标签页的界面消息列表仍是各自的内存状态。

## 6. 微信聊天的实际会话规则

微信通道使用：

- `source_channel=wechat`；
- 微信私聊或群聊对应的真实 `chat_id`；
- `CLAW_SESSION_POLICY_CHAT`。

所以扫码登录、网络重连、微信网关重新连接本身，不应自动产生新的聊天会话。只要还是同一个微信聊天对象，消息会继续落到同一个聊天上下文。

会产生不同上下文的典型情况是：

- 换了另一个微信私聊对象；
- 换了另一个微信群；
- 设备端删除了会话存储；
- 用户通过会话管理能力主动新建或切换会话别名。

微信和 Web **不会天然共用同一个会话**，因为它们的来源通道不同：

```text
Web：    source_channel=web    + chat_id=网页 UUID
微信：   source_channel=wechat + chat_id=微信聊天 ID
```

## 7. 后续建议的会话设计

建议在 Web 前端明确提供：

- 当前聊天名称或编号；
- “新建对话”按钮；
- “恢复历史”按钮；
- “清空页面显示”与“删除设备端会话”两个不同动作；
- 断线重连时显示“已恢复原聊天”，而不是只显示“在线”；
- 页面刷新后从设备端或浏览器 IndexedDB 恢复消息列表。

这样可以把“网络连接重连”和“聊天会话切换”彻底区分开，避免用户误以为每次断线都丢失上下文。

## 8. 当前验证边界

- 已通过源码确认 Web `chat_id` 使用 `localStorage` 持久化，WebSocket 重连复用原 ID。
- 已确认 Web 通道固定使用 `web`，微信通道使用 `wechat`，两者默认分开建会话。
- 已确认页面消息列表当前只在内存中，刷新后不会自动恢复显示。
- 本记录只整理体验和会话机制，没有在本轮实施前端改造。
